package com.automan.siberia.sort;/** * @Author he.zhou * @Date 2021-04-22 */public class DuTest {    /**     * 9 10 11 1 2 3 4 5 6 7     *     * @param arr     * @param target     * @return     */    public static int testfanzhuan(int[] arr, int target) {        int start = 0;        int end = arr.length - 1;        while (start <= end) {            int mid = (start + end) / 2;            if (arr[mid] == target) {                return mid;            } else {                //mid落在左边递增                if (arr[mid] > arr[end]) {                    if (arr[mid] > target && arr[start] <= target) {                        end = mid - 1;                    } else {                        start = mid + 1;                    }                } else {                    //mid 落在右边递增                    if (arr[mid] < target && arr[end] >= target) {                        start = mid + 1;                    } else {                        end = mid - 1;                    }                }            }        }        return -1;    }    public static void main(String[] args) {        int[] arr = new int[]{9, 10, 11, 1, 2, 3, 4, 5, 6, 7};        System.out.println(testfanzhuan(arr, 7));    }}